import os
flags = {d} #-d means depth-limited

listdir()


queue = []
directory = queue.pop()
for filename in directory:
	
	if is_directory(filename):
		queue.add((filename, level))


if __name__ == "__main__":
	if argv[0][0] == '-':
			
